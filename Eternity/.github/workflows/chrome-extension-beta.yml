name: chrome-extension-beta

on:
  workflow_dispatch:
  push:
    paths:
      - "src/main/java/websocket/manifest.json"
      - "src/main/java/websocket/content.js"
      - "src/main/java/websocket/pageBridge.js"
      - "scripts/package-chrome-extension.ps1"
      - ".github/workflows/chrome-extension-beta.yml"

jobs:
  package:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Package Chrome extension
        shell: pwsh
        run: ./scripts/package-chrome-extension.ps1 -OutputDir artifact/chrome-extension

      - name: Upload beta artifact
        uses: actions/upload-artifact@v4
        with:
          name: roll20-bridge-chrome-extension-beta
          path: artifact/chrome-extension/*.zip
          if-no-files-found: error
